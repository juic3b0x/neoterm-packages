--- a/src/update.c
+++ b/src/update.c
@@ -360,7 +360,7 @@
 
   tmpdir = getenv("TMPDIR");
   if (tmpdir == NULL) {
-    tmpdir = "/tmp";
+    tmpdir = "@NEOTERM_PREFIX@/tmp";
   }
 
 #ifdef O_TMPFILE
